<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a1117">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="最短距離ゲーム"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.1117">
        <activity android:name=".fivefiveWallActivity"></activity>
        <activity android:name=".fivefiveAnswerActivity"></activity>
        <activity android:name=".resultActivity"></activity>

        <activity android:name=".startWindowActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>